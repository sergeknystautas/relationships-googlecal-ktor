<h4>Known issues</h4>
<ul>
<li>Authorization should but <a href="https://youtrack.jetbrains.com/issue/KTOR-2128">does not automatically update</a>.  After an hour or so, you'll get an error, and have to logout and log back in.</li>
<li>Calendar data is cached, but the first time loading can be slow.</li>
<li>Some types of meetings are counted as 1:1s when they're not.</li>
<li>Kotlin's serialization does <a href="https://github.com/Kotlin/kotlinx.serialization/issues/204">not support streams</a> making the intended pattern for calendar serialization impractical for large calendars.</li>
</ul>
